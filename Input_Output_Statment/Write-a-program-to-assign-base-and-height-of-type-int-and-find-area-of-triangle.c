#include <stdio.h>

int main() {
    int base, height;
    float Area;

    base = 5;
    height = 10;
    Area = 1.0 / 2 * base * height;

    printf("Area of Triangle = %f\n", Area);

    return 0;
}
